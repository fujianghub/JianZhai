# 简斋 · 版本历史

> 阶段交付记录。日常「坑 / 决策复盘」在 memory（见 `MEMORY.md` 索引）；逐 commit 见 `git log`。

| 阶段 | 内容 |
|---|---|
| **v0.1 MVP** | KB + Folder + Document CRUD + Markdown 编辑器 + 私密/公开切换 + 博客前台 |
| **v0.2 编辑器增强** | Tiptap 富文本、`/` 命令、块拖拽、`@提及` 双向链接、反向链接区 |
| **v0.3 协作辅助** | 历史版本（diff / 回滚）、PG 全文搜索（jieba） |
| **v0.4 导出能力** | 单文档/文件夹/KB 导出，HTML/MD/PDF/DOCX + 整站 zip |
| **v0.5 完善** | 评论、标签云、归档、RSS、移动端适配 |
| **v0.6 AI + 编辑器扩展** | apps/ai、玄黑玻璃后台；MathNode / DetailsBlock / Columns / Tabs / DocCardEmbed / FontSize / Indent / 上下标 / BlockHoverMenu / 图片悬浮工具栏 |
| **v0.7 UI 打磨** | 白边修复、博客 sticky 顶栏 + 卡片质感、4 主题适配、印章 favicon、PWA |
| **v0.8 编辑体验** | 大纲固定 / 全屏退出 / 全屏目录 / 所有编辑模式 AI / 路由 bug |
| **v0.9 视觉系统** | 自制 SVG 图标库 + 双色调彩点 + 主题联动染色 |
| **v0.9.1 维护** | 搜索含标签/评论；链接租户边界；原子 version；AI max_tokens；编辑器竞态修复 |
| **v0.9.2 MD/图表 + 架构** | KaTeX 全链路；Mermaid/PlantUML 默认渲染 + 单击切源码；docN 链接重写；linking 锁修复；AI 服务端长度上限；exporter CSS 线程安全；prod SECRET_KEY 兜底 |
| **v0.9.3 HTML/Mermaid + 安全下载** | HtmlPostReader 懒加载 + 异步元数据 + sessionStorage LRU；Mermaid 全屏 Modal；downloadExport 改原生 a href；`pnpm dev:https`；AI 用量日历热图 + pricing.py |
| **v0.9.5 Hero + 组织** | 首页 Hero 名句轮播（朝代/作者/篇名 + 4 动画 + 批量导入 + `/admin/hero`）；KB 大类分组、文档置顶、收藏夹、多种排序；回收站 UI |
| **v0.9.7 AI 全面增强** | 多供应商（Claude + 通义千问）；自定义模板；多轮对话；视觉输入；扩展思考；每用户日预算（429）；失败降级链；prompt caching；用量归因 + CSV |
| **v0.9.8 部署 + 友邻闸门** | 腾讯云部署套件 `infra/`；`SITE_REQUIRE_LOGIN` 友邻可见闸门（`PublicOrLoginGated`） |
| **v0.9.9 账号体系** | 根管理员分级（不可被禁用/删除）；新建账号邮箱必填；账号自服务（改密码/邮箱/用户名/头像）；KB 上传进度条 + 批量全选 + 颜色选择器 |
| **v0.9.10 题记增强** | 播放顺序可配置（random 默认洗牌）；悬停暂停 + 点击切换；dnd-kit 拖拽排序；导出备份文本；「首页题记」改名「题记」 |
| **图标体系定稿** | 三区三语言：侧栏 `JzIconKit`（15 枚淡染裸放 + tone 十色）；博客顶栏回最初版浅染族；主题四枚回 AntD；PostDetail 全换自制图标，卸载 hugeicons |
| **MD 编辑器换 CM6**（2026-06-07） | MD 源码 textarea → CodeMirror 6（语雀级）；`EditorSurface` 适配层；vite manualChunks 拆 chunk；lang-markdown 懒加载多实例坑 → `resolve.dedupe` + `optimizeDeps.include` 根治 |
| **追赶语雀第二批**（2026-06-07） | 富文本表格单元格底色/文字色 + 条件 HTML 序列化；悬浮行列增删 + grip 选行列/拖动重排；MD Live Preview；表格浮动操作条 |
| **安全复审批次**（2026-06-07） | 六领域复审修复合并 main（a72e516）：TLS 条件硬化、友邻闸门加固、`raw_content` 泄漏封堵、AI 预算调用前预留、iframe 去同源 |
| **编辑器高危修复 + 表格保真**（2026-06-07） | 表格冻结首行/首列（三端 sticky）；`convertLayoutBlocks` 根治 callout 劫持 details/cols/tabs；`.jz-table-wrap` 滚动容器；Mermaid 净化修复 |
| **性能优化 9 Phase**（2026-06-08） | defer 大正文字段；软删复合索引；消除 N+1；AISettings 单例缓存 + 预算 DB 聚合；公开聚合缓存；懒加载 pdfjs+mammoth；富文本打字防抖；静态站流式写盘 |
| **布局 + 导出保真**（2026-06-08） | 完整编辑两栏铺满；Mermaid 离线导出 SVG（headless Chromium + vendored mermaid.min.js） |
| **MD 本地图片打包**（2026-06-13） | 导入 `.md` 的 `![](./images/x.png)` 相对图不再 404；三入口共用 `_bundle_import_entries`（图片→MD 附件 + 改写为 `/media/`）：整文件夹上传 / 两步选择器 / ZIP 导入；`import_local_images` 命令补图 |
| **四角色权限体系 RBAC**（2026-06-21） | 根/管理员/普通用户/匿名（`get_role` 唯一入口 + `IsContentAuthor`/`IsRoot`）；`scope_queryset` 由 owner 隔离改**作者共享单一池**；普通用户=读者；删除分级（删 KB/大类/永久删/清空回收站=仅根）；迁移 0006 降级历史非根超管；权威清单 `docs/permissions.md`。合并 main（e26baf7） |
| **登录三因子 + 滑块验证码**（2026-06-23） | 登录从「用户名+密码」升级为三因子：+ **邮箱匹配**（须等于账号 `User.email`）+ **服务端古风拼图滑块验证码**（`captcha.py` Pillow 程序化生成、6 古风色板×多背景样式随机、经典拼图块、答案存 Redis 一次性、缺口仅由像素传达防脚本）；前端 `SliderCaptcha`（1:1 像素拖拽 + 进度填充）；登录页去 label 紧凑化 + 线条图标焦点高亮 + 整体放大约 120%。无新模型/迁移。合并 main（e3c0a41） |
| **阅读排版定制 + 专注模式**（2026-06-26） | 博客读者侧阅读设置收为分组工具条胶囊（字体/纸张/排版/专注，与编辑钮等高 28px）；**排版三件套**（`readerLayout.ts` + `ReaderLayoutPicker`）字号缩放/行距/版心宽度 + 一键重置，落 localStorage、以 CSS 变量 scope 到 `<article>`、仅 Markdown 阅读路径消费（HTML iframe/二进制预览不参与）；**专注/沉浸阅读模式**隐藏导航栏与侧栏、Esc 退出；阅读进度条加百分比；图标语义化（字体=FontColors、纸张=File、专注=Eye）。纯前端，无后端改动。合并 main（76fb9ad） |
| **默认要求登录**（2026-06-27） | `SITE_REQUIRE_LOGIN` 默认值翻为 `true`：不登录看不到任何文章，访问任意页面直接跳登录页（沿用既有 `PublicOrLoginGated` + `BlogLayout`，无新模型/迁移）；补 `/d/:id`（`DocLinkResolver`）匿名跳登录守卫；测试钉住新默认 + 匿名公开端点用例显式 `override_settings(False)`。`.env` 设 `SITE_REQUIRE_LOGIN=false` 可切回全开放 |
| **用户标签 + KB/大类朋友圈式可见性**（2026-06-27） | **用户标签** `UserTag`（全局共享，反向名 `account_tags` 避让内容标签 `apps/tags`）：`/auth/user-tags/` CRUD + `UserSerializer` `tags`/`tag_ids` + 用户列表按标签/搜索筛选；标签对读者隐藏。**KB/大类三态受众** `audience_mode`（`all`/`exclude`/`include`，默认 `all` 向后兼容）+ `audience_users`/`audience_tags`（按用户+标签定向）：新建 `apps/knowledge/audience.py` 统一收口**全部读者入口**（列表/详情直链/树/归档/RSS/sitemap/相关/反链/收藏/评论），作者（`is_staff`）绕过；文档可见 ⇔ KB 可见且（无大类或大类可见）；匿名白名单不可见、黑名单可见。后端守卫：受众名单含管理员（作者）→ 400（防误触）。前端 `AudienceControl` 复用组件 + `UsersPage` 标签列/筛选/标签管理。迁移 `accounts 0007` / `knowledge 0007`；新增 22 测试 |
| **章节自动编号 + 目录生成（语雀式）**（2026-07-02） | **① 章节编号 = 显示层**：序号不写入 `raw_content`/`published_content`，源码保持干净；共享算法 `utils/headingNumber.ts`（栈压缩，按**嵌套深度**非 markdown 原始级数：`h1→h2→h4 = 1/1.1/1.1.1`），四端复用——阅读器（markdown-it `heading_open` 注入 `<span class="jz-heading-num">` + `TocEntry.numbering`）、CM6 源码（`extensions/headingNumber.ts` widget）、Tiptap 富文本（`HeadingNumber.ts` node decoration `::before`）、大纲/目录面板；每篇文档开关 `Document.heading_numbering`（迁移 `knowledge 0008`）+ 编辑器工具栏 Switch。**② 目录生成**：全文 `[TOC]`（已有）+ 新增 `[TOC:section]` 本节子树目录（`expandTocPlaceholders` 位置感知，复用 `heading_open` 已定锚点 id）；斜杠 `/目录`、`/本节目录` 双端可插。**③ 导入选项**：上传下拉两复选框（章节编号 / 文首插入全文目录）→ `views.py` `_parse_import_options` 落地。**④ 导出端对齐**：`exporter/services/markdown_render.py` 补齐 heading 锚点 + 编号栈 + `[TOC]`/`[TOC:section]` 展开（离线 HTML/PDF/静态站，读 `doc.heading_numbering`）。**⑤ 普通编辑（内联）修复**：`PostInlineEditor` 补编号开关 + 修 raw/published 断层——博客内联「编辑」原只写 `raw_content` 而博客渲染 `published_content`（故意不同步）致内联插的目录/编辑上不了博客；新增 `documentSave.ts` `patchDocumentBody` 一次 PATCH **双写**两字段（`_apply_update` 收两字段只 bump 一次 version）。新增前端 15 + 后端 11 测试 |
| **阅读字号滑块 + Word 一体化 + PPT 阅读器**（2026-07-04） | **① 阅读字号滑块 50–150%**：与排版三件套步进器共驱 `--jz-reader-scale`。**② Word 一体化保真导入**：`services/docx_import.py` 用 mammoth 抽 `.docx` 正文，修复 docx 正文**从未真正提取**的 latent bug（mammoth 需 `BytesIO` 而非裸 bytes）；表格/图片保真（图片落 Attachment 并改写引用），docx 走 MD 阅读管线（目录/排版/内联+完整编辑）。**③ PPT 有道云式阅读器**：`editor/tasks.convert_pptx_to_slides`（Celery 幂等）`soffice --headless`→PDF→`pdftoppm` 逐页图 + `SlideImage` 模型（迁移 `editor 0002`）+ 前端 `PptxReader`（缩略图侧栏/键盘/缩放/全屏/转换态轮询）；线上镜像需加 `libreoffice`/`poppler`。合并推送 main（0653208） |
| **PPT 瘦身 + 损坏上传拦截 + 转换状态可见**（2026-07-10，3fa9ba9） | **① 光栅化改 JPEG**（`pdftoppm -jpeg quality=82`）+ 320px 导轨缩略图（`SlideImage.thumbnail`，迁移 `editor 0003`）：94 页 deck 从 ~24MB 降到几 MB。**② B1 损坏上传拦截**：OOXML 是 zip，`_is_valid_zip`（`zipfile.is_zipfile`）在入库前校验 `.docx/.pptx`，坏文件 **400** 拒收（soffice 加载坏源仍退 0，不能靠转换失败兜底）——「PPT 转换失败」多为原文件本就损坏。**③ B2 转换状态可见**：`Document.slide_status`/`slide_error`（迁移 `knowledge 0009`）持久化转换态，`PptxReader` 区分 pending/failed、显真实原因、失败即停轮询。**④ 存量**：`manage.py reconvert_pptx` 回填旧 PNG/无缩略图 deck |
| **PPT 缩略图修复 + 备注展示**（2026-07-10） | **① 缩略图变横线修复**（纯前端）：`.jz-pptx-rail` 是有界 flex-column，~90 个缩略图按钮默认 `flex-shrink:1` 在滚动生效前被压扁到 ~4px、`overflow:hidden` 把图裁成一条线——后端数据本身完好；`PptxReader.tsx` 缩略图按钮加 `flexShrink:0` + img 补 `aspectRatio` 根治。**② 新增讲者备注**：原管线只 LibreOffice→PDF→JPEG 光栅化、从不解析 pptx 结构；新增 `python-pptx` 依赖 + `extract_pptx_notes()`（best-effort，失败不拖垮转换，按 index 对齐渲染页、隐藏页漂移则留空）+ `SlideImage.notes` 字段（迁移 `editor 0004`）+ `as_dict` 带出（两序列化器自动生效）；前端 `PptxReader` 主图下方可折叠**备注面板**（工具栏「备注」开关、逐页显示、空页「此页无备注」、可复制、全屏也支持）。**③ 存量回填**：`manage.py backfill_pptx_notes`（只读源文件补 notes、不重新光栅化）。新增后端 4 测试；生产需重建镜像（`python-pptx` 新依赖）+ migrate + `backfill_pptx_notes --all` |
| **Word 字体色保真 + 语雀 MD 远程图修复**（2026-07-12，a982c02） | **① Word 导入字体色**：mammoth **默认丢弃 run 级直接颜色**（`w:rPr/w:color`）致导入彩字历来全丢；`docx_import._mark_run_colors` 转换前在 docx XML 层给带色 run 包 `jzcolor<hex>b…jzcolore` 哨兵（mammoth/markdownify 当普通文本原样带过），最终 md 用正则换回 `<span style="color:#hex">`，表格内彩字同样保真（**导出端彩色仍丢为已知限制**）。**② 语雀 MD 远程图不显（双因叠加）**：`cdn.nlark.com` **防盗链**——浏览器带外域 `Referer` 直连远程图 403 图裂 → 前端 `addImgLazyAttrs` 注入 `referrerpolicy="no-referrer"`（**服务端 fetch 无 referer=200、浏览器直连=403** 是关键区别）；**同步镜像超时**——上传请求内串行下载 40+ 张远程图（CDN 每张限流 5–15s）超请求超时被中断致图仍是远程 URL → 改 Celery 异步 `mirror_document_images`（`views` 里 `.delay()`，仅含需镜像远程图才派发）+ `ThreadPoolExecutor(6)` 并行（实测 42 张 73s 全落地）。后端 editor 58 + 前端 markdown 71 测试绿 |
| **双击图片放大预览**（2026-07-17，34d137f） | **修复 lightbox 从未生效的时序 bug**：`useImageLightbox` 的 `useEffect` 依赖 `[containerRef]`（稳定引用只跑一次），而 `PostDetail` 在文章异步加载完成前先 `return <Spin/>`，首次挂载时 `articleRef.current` 为 `null` → effect 早退且**永不重绑**，点击委托从未绑上、图片预览实际从未生效。**修**：重写为 **`selector`+`bindKey` 范式**（对齐 `TableEnhancer`/`CodeBlockEnhancer`），依赖 `[selector, bindKey]`，内容落地即重绑。**交互**：改 `dblclick` 双击触发；遮罩支持滚轮缩放/拖拽平移/Esc/背景关闭（复用图表全屏工具栏样式）；抽出纯判定 `shouldOpenLightbox()`，跳过 `<a>` 内图片与 `data-no-lightbox`。**接入 4 个渲染面**：`PostDetail`(`.jz-post-article`) / `LivePreviewPane`(`.jz-doc-live-preview`) / `PublicAttachmentPreview`(`.jz-att-md`) / `FilePreview`(`.jz-file-preview-md`)；`reader.css` 缩放遮罩样式 + `cursor:zoom-in` 扩展到全部面。新增纯逻辑 5 + happy-dom DOM 集成 3 测试（复现异步挂载→双击→遮罩），新增 `happy-dom` devDependency；342 测试 + tsc + build 绿 |
| **语雀 MD 导入识别错乱三 bug 修复**（2026-07-19） | **① 图表注释被 `-->` 截断（主凶）**：语雀把 mermaid 导出为 `<!-- 这是一个文本绘图，源码为：… -->` + 静态 SVG；通用注释剥离的懒惰正则在源码内部箭头 ` --> ` 处提前截断 → 图表源码泄漏成正文、`:::jam` 触发失控 callout 吞后文。修=`recoverYuqueDiagramComments`（前端）/`recover_yuque_diagram_comments`（后端导出镜像）：闭合锚定「`-->`+行尾」，注释还原成原生 ```` ```mermaid ```` fence 并丢弃静态 SVG，须在通用剥离前运行。**② `<font>` 交替误合并**：「整句染色+局部加粗」被 `normalizeYuqueEmphasis` 步骤 (0) 连接符 `[^*\n]+?` 吞成整句全粗 → 收紧 `[^*\n<]+?`。**③ CJK 双加粗吞并**：步骤 (1)（`**A**B**C**`→`**ABC**`）的无空格连接符 + `\w` lookaround 在中文全失效，两个合法加粗被吞并/静默删标记 → 整段删除（后端本无此步骤，删后前后端对齐）。真实文档（13 图）Playwright 实测 13/13 水合、零泄漏；前端 350 + 后端 exporter 84 测试绿 |
| **语雀式链接三形态**（2026-07-20，ee1a492） | 链接可切 **链接（URL 原文）/ 标题（目标标题，默认）/ 卡片** 三形态 + **打开文档**（内链站内跳转）/**浏览器访问** 动作，全端落地。**① Tiptap**：`LinkBubbleMenu`（第二 BubbleMenu 实例，格式气泡在链接上让位）+ `LinkPasteAutoTitle` 粘贴裸 URL 自动异步取标题（href+旧文本双守卫防覆盖）+ 卡片 NodeView hover 回转菜单。**② CM6**：`pure/linkAt.ts` 纯函数 + `LinkFloatingMenu` portal 菜单（fence 感知）。**③ 阅读端**：`convertBlockPlaceholders` 补 `[[link-card:URL]]` 渲染（修字面量泄漏 + 编辑器重载丢卡片旧缺口）+ `CardEnhancer` 登录态水合（外链 OG / 文档真实标题，降级静态壳）。**④ 后端**：`link-preview` 放宽 `PublicOrLoginGated` + 独立限流 30/min，抓取抽 `services/link_preview.py`。**⑤ 导出**：`card_placeholders.py` 卡片渲染（HTML/PDF/site）/降级（.md/docx），无元数据兜底降级**零 `[[` 泄漏**；搜索索引剥卡片语法。**两个存量 bug 顺带修复**：Tiptap v3 Link 协议白名单剥 `doc:` mark（`protocols:['doc']`）、v3 组件读 editor 态须 `useEditorState`。后端 402 + 前端 384 测试绿 + Playwright 端到端 18/18 |
| **正文长图限高三段式**（2026-07-21，34ae1f7） | 阅读端超长图不再撑爆版面：CSS `70vh` 限高连续缩放 + 极端长图（高宽比超阈值）`LongImageEnhancer` 折叠为可展开预览 + 阅读设置面板开关；enhancer 的 dataset 处理守卫在 StrictMode 下失效，改 `WeakSet` |
| **六主题配色 + 界面动效批次**（2026-07-22，4e5d029） | **配色（P0+P1）**：新增 `--jz-on-accent`（六主题「accent 上的墨色」前景，翡翠墨/紫夜墨/海渊墨/春潭墨/雪夜墨）修复 Segmented/主按钮/印章白字最低 1.8:1；**星空/深海专属玻璃面**（紫调/青碧 0.72 实底 + 同色 border/棱线，根治亮部画布物冲淡面板正文）；春水/冬雪防漏翡翠 + muted 对比全面 ≥4.9:1；卡片 hover 五主题本色（朱砂双层影/紫辉/青辉/冷影/冰蓝描边）。**动效三梯队**：主题切换走 **View Transition**（默认交叉淡融，菜单点击处**圆形揭幕**，reduced-motion/无 API 回退瞬切）；**「随朝暮」**昼夜自动主题（昼 6–18 宣纸、夜星空，分钟对表，手动选主题即退出）；顶栏滚动态 `is-scrolled`（`useScrolled`）；首页 KB 网格滚动显现（`useRevealOnScroll`，IO + 批内 stagger，animation `fill:backwards` 不劫持 hover）；玻璃 **spotlight**（`PointerSpotlight` 单例委托写 `--jz-mx/--jz-my`，`.jz-book`/`.jz-card` 指针跟随 accent 辉光）；星空月亮按**真实月相**渲染（`utils/moonPhase.ts` 朔望月推算 + 椭圆明暗线，切换器 tooltip 显今夜月相）；**四主题彩蛋**（春水点击生涟漪 / 星空点击放流星 / 深海鱼避光标 / 冬雪光标扰雪，`PointerState.px/py/clicks` + 交互元素点击过滤）；light/dark **呼吸背景**（`@property` 注册变量驱动布局光晕 64s 漂移，顺带复活被 `background:…!important` 简写压死的光晕死代码）；两套 canvas 脚手架**自适应降质**（帧时 EMA 三档降粒子 + DPR，滞回 + 冷却）。**存量修复**：shaderCanvas StrictMode 双挂载 cleanup 同步 `loseContext` 打死同 canvas 唯一 GL context，致春水 shader dev 下每帧抛错——改延迟释放 + 重挂载取消。前端 409 测试绿（新增 moonPhase/resolveClockMode 8 例）+ Playwright 三轮实拍 |
| **LaTeX 数学全链路补全**（2026-07-23） | 「支持 LaTeX 并自动识别解析」第一批（审计 P0 全部 + 搜索剥离）。**① 富文本行内自动识别**：MathInline 补 InputRule（打完 `$x$` 即转公式节点，与块级对称；正则收敛进纯模块 `editor/mathPatterns.ts` 与 PasteRule 共享）。**② 反斜杠定界符归一化**：`\(..\)` → `$..$`、`\[..\]` → `$$..$$`（ChatGPT/论文来源），前端 `normalizeLatexDelimiters` 挂 `preprocessMarkdown`（阅读/富文本载入/粘贴三路）+ 后端 `markdown_preprocess` 镜像；块级锚定行首行尾避开转义方括号、inline-code/围栏守卫。**③ 导出端从残破到真实渲染**：`markdown_render` 装数学 tokenizer（escape 后 emphasis 前，根治公式 `_`/`*`/`\` 被 CommonMark 吃掉）+ 新 `math_render.py`（headless Chromium + vendored katex 批量预渲染，`build_scope_math_html` 穿线 HTML/PDF/静态站；字体 base64 内嵌 CSS 仅含公式才注入；缺 Chromium 降级源码 span）；docx 公式以 Cambria Math run 保留原文（无 OMML 为已知限制）。**④ 搜索索引剥公式**（先归一化再剥，LaTeX 碎片不再污染 tsvector）+ `reindex_search` 全量重建存量 181 篇。坑：Python `"" in "0123456789"` 恒 True，行首公式曾全体失效。前端 425 + 后端 423 测试绿 |
| **编辑器体检修复批次**（2026-07-24，d40e4bf） | 四路并行体检（Tiptap / CM6 / 阅读端 / 外壳数据流）后的第一批修复，11 项。**① 工具栏激活态陈旧**：Tiptap v3 `useEditor` 不随 transaction 重渲，主工具栏/气泡菜单/四个下拉在 render 里直接 `editor.isActive()` 是陈旧快照（光标移进/移出粗体高亮不更新）——全面改经 `useEditorState` 订阅（LinkBubbleMenu 同构推广）。**② Callout 标题保真**：`:::info 自定义标题` 阅读端渲染标题栏但编辑器 serialize 从不输出 → 富文本重载保存即丢；节点补 `title` 属性（`data-title` round-trip）+ serialize 输出 `:::kind 标题` + NodeView 显示标题栏 + validate 与阅读端对齐。**③ 409 冲突不再静默丢字**：本地内容先备份 localStorage（新 `utils/localDraftBackup.ts`）→ 弹「恢复我的编辑 / 使用服务器版本」，恢复后基于新版本自动重保存；卸载 flush 失败同样备份 + 提示；`beforeunload` 补 `returnValue`。**④ 选区 AI 回写**：润色/扩写/纠错/翻译在 MD/HTML 经 `EditorSurface` 一键替换原选区（发起时快照选区 + 应用前校验原文未变，防流式期间盲替换）；富文本不再挂 `SelectionAI` 去双入口；`DocAIPanel` 全文截断 2 万字 + capabilities 去重 + FAB 换真 Button。**⑤ 性能**：CM6 标题编号 `changeMayAffectNumbering` 变更门控（普通打字只平移装饰，不再逐键全文 O(N) 重扫）；光标移动去两处全文 `doc.toString()`（slash/链接检测改传当前行 + 偏移平移）；lineMap 缓存键补 numbering（开编号不再必然 cache miss）；`BlockHoverMenu` mousemove rAF 节流 + 同块短路。**⑥ 其余**：富文本 `@`/快捷键拦截补 IME `isComposing` 守卫；图片上传插入点 transaction mapping（上传中打字不再错位）；HtmlEditor Tab 缩进；LivePreviewPane 去冗余 `jz-post-article` 类（enhancer 错绑隐患）；VersionsDrawer 存版本/回滚/对比失败补错误提示；顶栏 ⋯ 按钮 tooltip/aria。前端 450（新增 25 例）+ 后端 423 测试绿 + Playwright 冒烟（激活态/标题 round-trip 实测）。体检遗留待议：暗色编辑器白底、`useBlocker` 需迁移 data router、HTML 模式升级 CM6、巨石组件拆分、`$5$` 货币守卫（四端镜像须同步） |
| **首次进编辑器崩溃修复**（2026-07-24） | 首次点击「编辑」（PostDetail 内联 / 完整编辑页首次懒加载富文本）必现 `Cannot read properties of null (reading 'commands')` 打到根 ErrorBoundary、重试后才正常。根因 = **Tiptap v3 `useEditor` 上游竞速**（`@tiptap/react` 3.23.4）：实例在 render 阶段同步创建并立刻挂 1ms 兜底销毁定时器（清理 StrictMode 丢弃渲染），首次懒加载挂载太重时定时器抢在 passive effect 前把实例 `destroy()`（`commandManager=null`），本次渲染闭包里的 editor 已死，挂载 effect 里 `editor.commands.setHeadingNumbering(...)` 即抛——**与 StrictMode 无关，prod 慢设备同样可触发**。修 = `RichTextEditor` 挂载 effect 守卫升级 `if (!editor || editor.isDestroyed) return`（setEditable / setHeadingNumbering / value 同步 setContent），`onEditorReady` 对已销毁实例传 null；实例被销毁时跳过即可，`useEditor` 会立刻重建并随 `[editor]` 变化重跑。Playwright mint-session 复现脚本修前 100% 崩、修后编辑器正常挂载可输入；前端 450 测试绿 + tsc 干净。详见 docs/editor.md §9 三坑之三 |
| **阅读端与后台 UX 修复第一批**（2026-07-24） | 两轮全量 UI/UX 审查（8 份报告，阅读端 + 后台 + 编辑器 + 响应式/无障碍）后的第一批落地，25 文件。**① 移动端阅读（根治死 FAB）**：TOC/KB FAB 在窄屏（<1281px / <1101px）与专注模式改开 AntD **Drawer**（点目录项自动收起，drawer 态刻意不持久化）——修复 961–1280px「FAB 点了没面板消费」的死按钮与 ≤960px FAB 被 CSS 隐藏致手机完全无目录的缺陷；专注模式放行 TOC FAB。**② 阅读位置记忆**：新 `utils/readingPosition.ts`（save/resume 双阈值带 + 200 条按时间剪枝，9 单测），回访浮「继续上次阅读 · N%」pill。**③ 阅读默认值**：默认字体 Verdana→宋体（Verdana 无 CJK 字形实为混排且悖宣纸调性）、默认版心满栏→860px（存量 localStorage 用户不受影响）；PaperPicker 加 `hiddenKeys` 隐藏被 blog-glass 中性化的三种暖纸（swatch 预览不再与实际不符）；字体/纸张控件仅对 Markdown 路径显示。**④ 读者/作者边界收口**：GlobalSearch 去 rank 分值 + 草稿 Tag 仅作者 + 空态提示/键盘滚动跟随；博客搜索非公开结果改走 `/d/:id`；DocLinkResolver 404 补返回首页；FavoritesPage 按角色隐藏编辑入口/状态 Tag、读者 KB 链接走 `/kb/:slug`、取消收藏加确认、admin 挂载去古风 hero；退出登录按路径分流；**侧栏收藏星标对登录读者开放**（原被 canManage 误门控）。**⑤ AI 死代码接线**：替换选中先过 `AIDiffPreview` 语义 diff 确认（原为静默覆写）；流式错误进面板分类 Alert + 「再来一次」真正可用（此前 error/重试 UI 与 diff 组件均已实现零引用）。**⑥ 后台快赢**：品牌默认色 `#1677ff`→`#10b981` 六处清扫；UsersPage 标签重命名 `window.prompt`→受控 Modal、新建密码 min 4→8；暗色主题变量泄漏修复（BacklinkPanel/CommentsPanel/DocHoverCard/TagPicker）；ExportsPage 失败态收敛单行 + PDF 提示去 `playwright install` 运维命令。**⑦ 外链**：阅读端 `https?://` 统一 `target=_blank rel="noopener noreferrer"`。前端 459 测试绿（新增 9）+ tsc + prod build + Playwright 双层冒烟（匿名三路由零运行时错误；mint-session 后移动端 FAB→抽屉/续读 pill 存取跳转/桌面 rail 共 9/9）。详见 docs/frontend.md §5 与 docs/ai.md §9 |
| **用户级知识库阅读权限白名单**（2026-07-24） | 用户管理里给普通用户配阅读授权 `accounts.ReadGrant`（迁移 `accounts 0008`：user × 恰一目标 FK，CheckConstraint + 条件唯一×4），四粒度：**整 KB（多选批量）/ 整大类 / 文件夹（含全部子文件夹，parent 树 BFS 展开）/ 单篇文档**。**空条目=不受限（向后兼容），有条目=白名单，与 KB/大类受众可见性取 AND**——enforcement 全在 `apps/knowledge/audience.py`（`_resolve_read_grants` memoize 于 user 实例；三个 `visible_*` 在受众过滤后追加 grant Q；`grant_documents_q` 供聚合注解、blog `_post_count_annotation` 以之收敛 KB 卡片计数），全部读者入口（列表/直链/树/收藏/评论/反链/RSS）自动生效零 view 改动；folder/doc 授权的宿主 KB+大类保持可导航，未授权内容（含 KB 名/文件夹名/计数）博客端零泄露；staff 不可被授权（400）、软删目标 fail-closed。API：`UserSerializer` 读 `read_grants` / 写 `read_grant_items`（全量替换、`[]` 清空、去重）+ `create()` 包 `transaction.atomic`（任一步失败不留半成品用户）。前端：新 `ReadGrantControl`（chips + 大类 Select + KB 多选 Select + 单选展开 `getKBTree` checkStrictly 树勾文件夹/文档）接入三入口（新建用户区块 / 行内眼睛按钮 / 「阅读范围」列点击直达）+ 表格「阅读范围」列（作者全库/不受限/受限(N)）。**顺修 dev 库偏斜**：`accounts_userprofile` 两个 NOT NULL 无默认列（未合并登录加固分支迁移残留）致建用户 500 且授权丢失——两列 `SET DEFAULT false` + 回填孤儿 profile。后端 448（新增 25）+ 前端 465（新增 6）测试绿 + Test KB 端到端实拍（四粒度/AND/防泄露/UI 全回路）。详见 docs/permissions.md §8 |
| **主题切换电影级过渡批次**（2026-07-25） | `stores/theme.ts` 重写为过渡指挥器 `transitionTo` **三拍法**：**① 分层错峰溶解**（默认）——背景（root）0.85s 先行、玻璃外壳（`jz-shell`）+120ms、顶栏（`jz-header`）+200ms 错峰跟进（电影 J/L cut），旧层 5px 失焦退场（rack focus）；分层 `view-transition-name` 由 JS 取 document 序首个匹配打 inline 名（纯 CSS 打名在编辑器嵌套 shell 时重名 → 整场 VT 静默跳过）。**② 柔边圆形揭幕**（菜单点击）——硬边 clip-path 改 `@property --jz-vt-cr` keyframes 内插驱动的 mask radial-gradient（90px 羽化，JS 半径 +90 余量），旧层退暗退饱和有进深；中线开卷式做过一版经确认撤除。**③ 随朝暮日落/日出长转场**——1.8s 慢溶解 + `.jz-dusk-veil` soft-light 暮色/晨光调色层（2.8s 起落自移除）+ canvas 2.5s 慢交接。**基础修复**：React 提交 `flushSync` 塞进 VT 回调（AntD 色板不再过渡后瞬跳）；`::view-transition{pointer-events:none}`（过渡期页面保持可交互）；`jz-vt-live` 关 body 底色 transition（new 快照是 live 的，否则双重动画）；快速连点 `skipTransition()` 防叠帧。**canvas 交叉溶解**：新 `AmbientStage` + `useSceneHandoff`（旧场景保活淡出 1150/2600ms 与新场景淡入交叠，根治切换硬切空一拍；四场景组件改受控 `active` prop，不再自读 theme store）。**AntD 主色单源化**：`MODE_ACCENT` 色表删除、运行时读 `--jz-accent`（`utils/themeAccent.ts`）——两处硬编码曾实际漂移（dark JS `#02b377` vs CSS `#2ee79c`）。前端 478 测试绿（新增 13 例）+ Playwright 真机冒烟 16/16 |
| **编辑器与文档预览样式优化批次**（2026-07-25） | 四路并行样式调研（工具栏控件 / 阅读端渲染 / CSS 架构与主题 token / 三编辑器内容区）后的第一批落地（12 文件，纯样式无迁移）。**① P0 修复**：富文本**章节编号从不显示**——折叠箭头 `::before` 特异性压过编号 `::before`，箭头迁 `::after`（`::before` 归编号专用）；表格**列宽拖拽把手补样式**（prosemirror-tables 官方 CSS 未随 @tiptap/extension-table 注入，把手曾是无定位裸 div）；**图片上传状态接工具栏 Tag**（`setUploading` state 曾被丢弃、上传全程零反馈）；查找当前匹配项前景 `--jz-surface`→`--jz-on-accent`（暗色下近透明不可读）；gapcursor/dropcursor 主题色（库默认硬编码黑、暗色不可见）；**纸张预设在后台被刷白**——裸 `.tiptap-shell` 选择器 + `!important` 把 paper-rice/kraft 全部中性化（与注释矛盾），收窄 `:not([class*='paper-'])`；**打印解除 70vh 限高**（超长表格/代码块打印曾被硬裁、内容永久丢失）+ 隐藏深色代码工具条；全屏下查找面板 z-index 1000→1150；移动端工具栏收敛规则迁层叠末端 editor-ui.css（曾被后加载文件覆盖成死代码）；HTML 工具栏 `flex-wrap` 反压 `.jz-glass` nowrap。**② 暗色/portal 令牌**：新增 `:root` 逐主题令牌 `--jz-overlay-surface`（portal 到 body 的弹层拿不到 `.jz-glass` 作用域令牌——slash/emoji/块面板/AI 菜单/查找浮窗暗色下曾近全透明，全部换不透明 overlay 令牌）、`--jz-surface-raised` 补定义（曾悬空恒 fallback `#fafafa` 暗色白砖）、`--jz-cell-surface(-2)`（= surface 预合成 bg-app 的不透明换算值，表格 zebra/题签表头/hover 全换——**冻结首行/首列在 light/dark 半透明玻璃令牌下曾透底**）；`.jz-doc-shell-blog` 补进 editor-ui.css 暗色令牌组（博客完整编辑入口暗色 AI 面板曾 92% 白底）；气泡菜单背景/激活态/选区色作用域 `.jz-admin-glass`→`.jz-glass`（博客壳曾缺激活态）；图表全屏暗色改深底（mermaid dark 浅字曾配浅灰底不可读）；AI diff 暗色浅前景；亮色兜底块翡翠绿硬编码改 accent 变量（春水/冬雪曾撞色）；保存按钮/模式切换器白字改 `--jz-on-accent`。**③ 所见即所得**：编辑态标题（h1/h2 补下划分隔线、补齐 h4–h6）/段落/列表/引用块/裸 pre 底色逐项对齐阅读端；行内公式补编辑态选择器；分屏预览 `.paper.markdown-preview` 同元素选择器修复（后代选择器不匹配自身，宋体默认在预览曾不生效）；内联编辑去双 `.paper` 嵌套（44+44px 双 padding）+ `!important` 复活被 inline style 压死的版式 + 编辑态吃阅读态字体/字号/行高变量（按 E 不再字体跳变）；danger callout 纠真红（曾 accent 致「危险」块渲染为绿）+ 补 `error`/`color6` 对齐导出端；链接双规则收敛（无差别 `a` 规则改仅 doc-link/脚注）+ 通用规则排除链接卡片（整卡 accent 底边污染）。**④ 无障碍**：编辑器/阅读端 14 类自定义按钮补 `:focus-visible` 焦点环、全局搜索恢复键盘焦点指示、`prefers-reduced-motion` 静态兜底（此前两项在编辑器/阅读端范围内均为零）。**⑤ 行内代码统一**：新增派生令牌 `--jz-code-inline-fg/bg/border`（fg = `color-mix(accent 54%, --jz-text)`，`:root` 单点定义随主题自动重算；纯 accent 亮色白底仅 2.7:1）——五镜像点对齐（阅读端/编辑器/表格内/AI 面板/**导出端** export-markdown.css 弃朱砂旧色）+ padding 改 em 随 50–150% 字号滑块缩放 + `box-decoration-break: clone` 折行保形；Playwright 六主题实测对比度 4.88–10.96 全过 WCAG AA。前端 478 测试 + tsc + prod build + Playwright 冒烟（19 项断言 + 六主题对比度）全绿；含 exporter 静态 CSS 一处（**已于 2026-07-26 随标准路径部署上线**，无新迁移）。深度调研遗留（token 双写收敛/三暗主题硬名单/表格双重定义合并/ToolbarDropdown 统一/z-index 量表）见 memory `project_style_batch_2026-07-25` |
| **语雀空格包裹公式抢救**（2026-07-26，81846d9） | 语雀导入公式显示为纯文本（doc 516《NUMA 架构》`$ [ B_{\text{total}}\approx2B ] $` / doc 503《AI Infra》`$ MFU = \\frac{…}{…} $`，全库仅 2 篇）。根因三层叠加：`$` 内侧留白撞四端一致的货币防误判边界规则（非解析器 bug）+ `[ … ]` 为 ChatGPT `\[..\]` 丢反斜杠残骸 + `\\frac` 语雀转义。修=`applyYuqueCompatMode` 首步 `rescueSpacePaddedDollarMath`（前后端镜像）：仅抢救「独立成行+内侧留白+含 LaTeX 信号(`\cmd`/`_{`/`^{`)」→ 多行 `$$` 块，还原 `\\`+字母转义、剥整体包裹的裸 `[ … ]`（区间形态保留）；货币无信号不触发，四端 tokenizer 未动；渲染时预处理故存量即生效。两端新增 11 测试；exporter 116 + 前端 70 + tsc 绿；两篇真实文档过管线渲出公式验证。**2026-07-27 已部署上线** |
| **导出功能大修批次**（2026-07-27） | 四维审计（管线/保真/UX/安全运维）+ 用户反馈（多篇 PDF 无目录）后的全面落地，15 项任务。**① 默认命名规则**：新枢纽 `build_export_filename`（`ExportScope` 增 `scope_type/folder`）= `大类-知识库-[文档标题\|文件夹-N篇\|N篇]-时间(-site)`，大类空省略、selection 恰 1 篇按单篇携带标题、段截 60 字符防超 `filename` 255；导出历史页「目标」列补显实际文件名。**② PDF 目录三件套**：`page.pdf(outline=True, tagged=True)` 书签树（**实测 Chromium 147 outline 单独传静默无效，必须 tagged 同开**）+ 页脚页码 + print 模式封面页/可点击目录页（复用 anthology 树）+ 表格/代码块/图/callout 分页护栏 + `<details>` print 强制 `open`。**③ docx 大修**：真 Heading 1-6 样式（导航窗格/自动目录解锁）+ eastAsia 字体（正文宋体/标题雅黑）+ 图片内嵌（超宽等比缩放）+ GFM 管道表转真表格 + 带色 HTML 表格提取文字矩阵 + callout/删除线/任务列表降级（gfm 自带 tasklists，checked 在 `list_item_open.meta`）+ 多篇封面与 Word TOC 域（fldSimple，F9 生成带页码目录）+ HTML 文档逐块成段（原折叠单巨段）。**④ 止血修复**：单篇 md 含媒体 zip 图片路径未改写（解包全裂）+ HTML `<img>` 媒体纳入收集/改写；静态站 `search.js` 未转义 `doc.title` 存储型 XSS（改 DOM `textContent`）；静态站 raw_content 整体回落**删除**（fail-closed 对齐博客端，feed.xml 改已发布纯文本 + guid/lastBuildDate，doc scope 补 `only_published` 尊重）。**⑤ 布局块后端镜像**：`convert_layout_blocks`（cols/tabs/details → 与前端同构 HTML，接 `preprocess_markdown` 步骤序）救 HTML/PDF/site 三格式塌缩与 `::col`/`::tab` 字面量泄漏 + 配套导出 CSS；表格密度/间距 CSS 变量恢复生效（export-markdown.css 写死 padding 曾压掉 `--jz-cell-pad-*`）。**⑥ 静态站增强**：共享 `style.css`/`katex.css`（原每页内联、含公式站点每页重复 ~420KB 字体）+ 树形导航（`render_toc_list_html` 增 `doc_href` 参数）+ sitemap/RSS 绝对 URL + `#doc-N` 死锚改写为页面文件名 + 导航 O(N²) 去重。**⑦ 运维三无补齐**：Celery `task_time_limit` 600s；`LOGGING` 配置（跨作者下载审计日志此前被 lastResort 丢弃）；`export_create` 限流 10/min（仅 create，list 轮询不受限）；同参 pending/running 去重复用（200 返回既有任务）；`cleanup_exports` 三段清理（TTL 7 天/僵尸标失败/孤儿文件，`manage.py` 命令 + beat 每日，`backups/` 永不触碰）；exporter 测试 autouse 隔离 MEDIA_ROOT（此前污染真实 exports/ 累积 566 孤儿文件）。**⑧ 内存**：md 多篇改 `stream_zip_to_path` 流式；媒体单文件 `EXPORT_MAX_ASSET_BYTES` 200MB 上限（超限保留 /media/ 原链）；scope 查询补 `select_related`。**⑨ only_published 端到端**：serializer 可写字段（存 selection JSON 免迁移，site 恒强制 true）+ ExportDialog 开关。**⑩ UX**：编辑器导出补跳转导出历史；轮询报错静默退避（原每 2s 刷 toast）；批量删除 `allSettled` 并发 + 失败保留勾选；失败行重试按钮；格式/状态过滤 + 耗时列 + MB 单位。**⑪ infra**：`backend.Dockerfile` 补 `.[pdf]` + `playwright install --with-deps chromium`（**此前生产镜像无 Playwright：线上 PDF 必失败、mermaid/KaTeX 静默降级源码面板**，需重建镜像生效；**2026-07-27 已随镜像重建部署上线**，Chromium 二进制走 npmmirror）；compose celery 加 `-B`（beat）+ `mem_limit 3g`。后端 498（新增 36）+ 前端 484 测试绿 + tsc + prod build + 五格式真实数据冒烟（80 页 PDF 含封面/目录页/105 条书签/页码实拍）。暂缓：EPUB / 目录页两遍渲染页码 / 全局完成通知 / 附件与 PPT 页图进包 |
| **多篇导出三层目录体系（PDF+Word）**（2026-07-27） | 用户规格：边栏「目录+各文档」层级书签 / 卷首层次目录可点击 / 每篇开头本篇目录。**PDF**：书签语义三件套——封面标题 div 化（不进书签树）、目录页标题升 h1、正文标题 `heading_shift` 降一级输出（锚点/编号仍按原始层级）→ 顶层书签恰为「目录+各文档」，篇内标题嵌套其下；卷首目录页每篇文档条目下嵌套 h1–h3 标题子条目（`render_toc_list_html` 增 `doc_sublist` 回调）；print 模式每篇 header 后注入「本篇目录」（h1–h4，正文自带 `[TOC]` 的不重复注入）。**跨篇锚点撞车根治**：`render_markdown` 增 `anchor_prefix`（`d{doc.id}-` 按篇命名空间）——多篇同名标题此前锚点重复、目录跳错篇（interactive 同受益）；增 `toc_out` 外传标题树。**HTML 格式文档兼容（关键）**：Word/模板导入文档整篇是原生 `<h1-6>`，从不经 markdown 渲染钩子——书签顶层被污染、目录全空；新 `_process_html_headings`/`process_html_headings` 让 html_block 与 HTML 文档 print 路径同获锚点/收集/降级 + 文档标题头。**Word 对齐**（`docx_export.py`）：正文标题降一级（Heading 2 起，文档标题独占 Heading 1 → 导航窗格/TOC 域按篇嵌套）；每个 h1–h4 挂 Word 书签（`w:bookmarkStart`，id 全文档唯一）+ 每篇 meta 后回插可点击「本篇目录」（`w:hyperlink w:anchor` 内链，先渲后插避免两遍扫描对齐风险，单篇不注入）；HTML 格式文档 heading 感知发射器（`<h1-6>` 转真 Heading + 书签，此前导航窗格对其完全失明）；`[TOC]` 占位符剥离（此前字面量泄漏）。真实数据实拍：85 页 PDF 顶层书签恰为「目录+3 文档」107 条层级嵌套、卷首 49 条子条目；docx Heading 1 恰为 3 文档标题、103 书签+103 内链、本篇目录 3/3。后端 506 测试绿（两批净增 8）。**2026-07-27 已部署上线** |
| **语雀兼容正则腐蚀 URL 链接修复**（2026-07-28，35e9138） | 症状：HillStone 链接设置标题后正文显示原始 markdown 残骸（`\[…\](<…%25257C_\_> \__0)`）。根因：`normalizeYuqueEmphasis` 步骤 (2) `/_{4,}/g → '__ __'` 相邻加粗拆分对 fence 外全文（含链接目标 URL）无差别生效——`TocPath=…_____0`（5 连下划线）被插空格+丢一下划线 → 链接失效退化纯文本 + linkify 部分自动链接 → **每次保存序列化回 `raw_content` 腐蚀一层**（逐轮升级、第 3 轮不动点；「链接标题」功能无罪，任何保存都触发）。实证：happy-dom + 无头 Tiptap 模拟「打开→保存」往返，第 1/2 轮输出与库中 444/438 存储内容**逐字节一致**。修复：`normalizeYuqueEmphasis` 入口掩码 `https?://` 段、出口还原（星号版 `\*{4,}` 一并受护；后端镜像从无此步骤无需修）；数据重建 444（raw+published）/438（raw，published 腐蚀前已发布本就干净），version+1 + 搜索向量重建；458 五条同款链接从未损坏、显示自愈。回归四类升**五类**（新增：URL 含 4+ 连续下划线/星号不被改写）。前端 488 测试 + tsc 绿；往返 4 轮不动点验证。**2026-08-03 已随纯前端快路径部署上线**（无新迁移；代码修复止损，线上库存量坏 `raw_content` 不自愈，待服务器侧重建或随下次内容同步带过） |
| **前端动效体系升级批次（Apple/鸿蒙对标）**（2026-08-04） | 三路代码探索 + HarmonyOS 7 联网调研后的 P0–P4 + B1–B6 全量落地（38 文件，纯前端无迁移）。**① 动效令牌机器化（P0）**：tokens.css 新增 `--jz-dur-fast/base/slow/epic` + `--jz-ease-standard/decel/spring/vt`（弹簧经 `@supports` 升级 `linear()` 真弹簧采样，旧浏览器留 cubic-bezier 近似）；theme.css 15 处散落曲线（弹簧曾三种书写）全部迁移；reduced-motion 补漏（卡片悬浮/光斑/进场/印章/登录光团/书卡此前全不在覆盖内）；五处重复 `prefersReducedMotion` 收敛为 `utils/motionPref.ts` 单一裁决点。**② 路由连续性（P1+B1+B4）**：新 `utils/routeTransition.ts`（VT 包装 + 目标页 ready 信号 + 600ms 超时兜底）+ `TransitionLink`（`Link` 连续性替身，修饰键/新标签放行）——博客端 5 页面 + 后台 AdminLayout 菜单/仪表盘/KB 列表全部内链接入，两 Layout 按 location 集中 `signalRouteReady`；首页书卡→KB 页做共享元素过渡（`jz-kb-hero`，KB 名/accent 经 route state 先行渲染页头壳承接；**源侧只命名被点击那一个元素**——同名两处=整场 VT 静默跳过）。**③ 玻璃活化（P2）**：spotlight 扩域后台功能卡；新增指针边缘光（mask 双层异或抠 1px 环，accent 光随 `--jz-mx/--jz-my` 游走）。**④ 交互粒子（P3）**：`utils/inkBurst.ts` 缃金墨点迸发——仅「加入收藏」成功触发（取消不庆祝），10 枚 ≤56px 一次性 DOM 动画播完自删，坐标取最近 pointerdown（键盘操作静默跳过）。**⑤ 动效档位 + 实况胶囊（P4）**：主题菜单尾部「动效·足量/适中/精简」三档（鸿蒙式用户主观调节；适中=canvas 质量钉最低档+关装饰光效/粒子，精简=等同 reduced-motion，CSS 侧 `[data-motion='min']` 伴生规则）；`ExportCapsule` + `stores/exportWatch` 导出实况胶囊（App 级左下角跨页常驻，2.5s 轮询到终态自停，done 停留 60s 可下载、failed 留驻显因）。**⑥ 骨架屏（B2）**：博客端三处居中 Spin 换保持目标布局几何的骨架（书卡网格/KB 列表/文章 860 版心），shimmer 走令牌、reduce/精简档静止。**⑦ 反馈就地化（B3）**：51 处 `message.success` 审计，删 2 处确凿冗余（KB 排序切换/AI 模型卡切换——UI 已自证）。**⑧ a11y 地基（B5）**：`.jz-sr-only` + `.jz-skip-link`「跳到正文」进两 Layout，Content 挂 `id="jz-main"`。**⑨ 收敛（B6）**：AI FAB 双份同名 `@keyframes jz-ai-fab-pulse` 删 tiptap.css 死份（加载序恒被覆盖）；胶囊 ≤720px 贴边。前端 518 测试绿（新增 37 例：motionPref/routeTransition/inkBurst/exportWatch）+ tsc + prod build + Playwright 双视口冒烟 + mint-session 端到端（书卡→KB 共享元素→文章全链路实测）。遗留待议：`transition: all` 10+ 处改显式属性、时长书写归一到令牌、`!important` 删除类清理需先搭六主题截图基线。**2026-08-06 已随纯前端快路径部署上线**（无新迁移；部署后验证绿三件全过） |
| **线上样式整段失效 hotfix**（2026-08-06，2da4a9a） | 部署动效批次后线上博客首页书卡区/阅读页样式裸奔（登录页/后台/dev 全正常）。根因：B6 删 tiptap.css 死份 `@keyframes jz-ai-fab-pulse` 时把 `.jz-doc-ai-fab{}` 块的**右花括号连带删掉**——dev 下每个 CSS 文件独立 `<style>` 标签、EOF 自动闭合无症状；prod 单 bundle 串接后，浏览器按 CSS 嵌套语义把 tiptap.css 之后 import 的 10 个样式文件（editor-ui/diff/paper/book-card/archive-tagcloud/reader/starry/deepsea/responsive/print）全部吞进 `.jz-doc-ai-fab` 作用域，选择器展平成 `.jz-doc-ai-fab .jz-book` 之类永不匹配，整段死亡。诊断路径：线上 Playwright 截图锁定书卡裸奔 → computed style `display:inline` 而规则本体在 built CSS 内 → 发现 `.jz-doc-ai-fab .jz-book` 污染选择器 → 花括号平衡扫描锁定 tiptap.css line 1298。修复=补回 `}` 一行；全量 styles 平衡扫描其余 14 文件干净；重建重部署后首页书卡网格/KB 页实测恢复。教训进 CLAUDE.md 动效层约定⑤ |
| **后台架构总览页内容刷新**（2026-08-06，0d3497d） | `/admin/overview` 静态文案多处停留在早期基线（owner 多租户、4 主题、完全匿名阅读、导出 4 格式、无 CM6），全面对齐现行架构：首屏简介改四角色 RBAC 共享内容池 + 默认友邻可见 + 受众/ReadGrant 两道闸 + AI 多供应商；技术栈三卡补 openai SDK、mammoth+LibreOffice 导入、Playwright 离线渲染、Pillow 验证码、CodeMirror 6、KaTeX/Mermaid、生产 Caddy 六容器；功能模块 12→16 卡（新增权限与可见性/登录安全/导入/题记）；详细版手写 SVG 同步（主题 6 套、安全盒 PublicOrLoginGated·IsContentAuthor·IsRoot + 三因子与限流、Celery 补 `mirror_document_images`、`exports/` 独立目录、AI 多供应商、「匿名读者」改「读者(需登录)」）；`simple-arch.mmd` / `detailed-arch.mmd` 同步刷新，`save-flow.mmd` 核对仍准确未动。tsc + prod build 绿。**2026-08-06 已随纯前端快路径部署上线**（JS hash 一致 / 匿名 403 / require_login 三项验证全过） |
| **导入体验批次：文件数上限 + 失败明细 + 目标文件夹**（2026-08-07） | 用户导入 HCIE-Datacom 大文件夹（文档+图片混合、超百文件）触发三连改进。**① 单请求文件数上限**：Django 默认 `DATA_UPLOAD_MAX_NUMBER_FILES=100` 拒收大混合文件夹组（`planUploadChunks` 对「文本文档+图片」同顶层目录**故意整组一个请求**发送——服务端要把 `./images/x.png` 改写为 `/media/…`，文档与图片必须同请求；普通组仍 8 个/片），`settings.py` 显式调至 **1000**（与前端整组发送逻辑耦合，两处改动须彼此对照）。**② 导入失败明细可见**：此前失败仅提示「N 个失败」、明细只落 console——`utils/notify.ts` 按 message 桥同套路补 **notification 桥**（App-scoped，主题/语言跟随），`uploadBatch.ts` 新增共享 `notifyImportResult`：全成功仍轻量 message；有失败/跳过改 notification 列出文件名+原因（>5 条折叠，失败时不自动关闭），`KBWorkspace` 三处 + `KBPostsPage` 统一接入。**③ 上传目标文件夹**：后端 `import_batch`/`import_file`/`import_zip` 一直支持 `folder` 参数但前端全部硬编码根目录——`KBWorkspace` 新增 `importTargetId` 状态：「新建 ▾」下拉加「上传到 [文件夹]」Select（复用 `folderOptions` 层级缩进，默认根目录），`KBTreeNav` 文件夹行新增「上传到此文件夹」按钮（设目标+直接拉起选择器），文件/文件夹选择器、拖拽、带图 MD、ZIP 五入口全部落到目标；目标被删自动回退根目录。**顺带核实**：单张图片成篇（`doc_format:image`）后端/阅读端预览均支持——「图片导入失败」为个例非能力缺失；图片与文本文档同组导入时按设计转为文档附件不单独成篇。tsc + 前端 518 测试绿 + 后端 506 测试绿。**2026-08-07 已部署上线**（含后端 settings 标准路径，无新迁移，绿三件 + 容器内 settings 实测全过） |
| **KB 页默认视图：按文件夹分组 + 列表**（2026-08-08） | 博客端知识库文档页（`KBPostsPage.tsx` KbBody）密度默认 `summary`（摘要卡片）→ `list`（标题+标签列表）；分组维度不动（原本就默认「有文件夹按文件夹分组、无文件夹平铺」）。**坑**：页面挂载 effect 会把当前密度自动回写 localStorage（`jz-kb-density:<slug>`），所有老访客都已存着旧默认 `summary`——只改代码默认值对他们永不生效；修 = 存储 key 升 `jz-kb-density-v2:<slug>`（旧值失读、一次性重置既往手动选择，此后正常记忆）；分组 key `jz-kb-view:<slug>` 未动。tsc 绿（该默认无测试覆盖，未跑 vitest 避免主 dev server `.vite` 缓存 desync）。**2026-08-10 已随字体批次部署上线** |
| **题记管理升级 + 心境时间戳**（2026-08-10） | **① 后台列表展示态改造**：`/admin/hero` 题记列表从「6 列常开输入框」改为样式化展示行（正文衬线 15px + 复用首页 `.jz-hero-cite-*` 三色署名，列表即逐条预览），点行/编辑按钮才切该行为编辑态，提交时机从每字段 blur 改为**焦点离开整行**（防 tab 切字段瞬间 PATCH 响应覆盖正在输入的内容）；**顶部草稿式新增**（正文留空不落库、首次填写并失焦/回车才提交置顶，不再产生「新题记」占位垃圾条目；草稿独立 state 防被他行提交响应抹掉）；搜索框按正文/朝代/作者/篇名过滤 + 表体 56vh 内滚动 sticky 表头；过滤激活时禁用拖拽排序。**② 心境时间戳**：每条题记新增 `created_at` 摘录日期（首页署名下金墨小字「摘录于 2026 · 8 · 10」，后台 DatePicker 可回填/清除；存量题记留空不显示、刻意不自动补今天）+ `updated_at` 内容修改日期（服务端在正文/朝代/作者/篇名变更时盖章、仅后台展示、公开端点不返回；仅回填日期不算修改）；stamping 收口 `hero.py _stamp_quote_dates`（PATCH 与批量两条写入路径都过）；批量导入/导出行尾可选 `@YYYY-MM-DD` 往返日期，批量缺省**不**默认今天（防 replace 还原旧备份伪造心境日期）。quotes JSONField 内实现，无迁移。后端 144 + 前端 527 测试 + Playwright 冒烟绿。2026-08-10 推 main，**2026-08-10 已随字体批次部署上线** |
| **题记多行支持**（2026-08-10） | 题记正文支持换行，首页与后台全链路：后台编辑正文 `Input`→自适应 `TextArea`（1–5 行），按键语义调整为 **Enter=换行、Ctrl/⌘+Enter 或失焦=保存**；首页 `HeroQuoteCard` 两级拆分（`\n` 分行堆叠 + 行内 ` · ` 分段圆点，印章跟末行，打字机动画逐段延迟跨行连续；`.jz-hero-quote` 转 flex-column + 新增 `.jz-hero-quote-line` 承接原行内布局，560px 媒体查询同步），单行题记视觉不变；后台列表展示态 `white-space: pre-line`；批量导入/导出行式格式用字面量 `\n` 转义换行（导出 `quotesToBatchText` 转义、后端 `_parse_batch_lines` 还原，备份完整往返，可与行尾 `@日期` 共存）；后端 `_validated_quotes` 归一 `\r\n`/`\r`→`\n`。后端 62 + 前端 528 测试 + Playwright 冒烟（两行正文提交/预览堆叠/API 往返）绿。2026-08-10 推 main，**2026-08-10 已随字体批次部署上线** |
| **字体系统全面升级：自托管 + 令牌化 + 文楷/MiSans**（2026-08-10） | 根因修复 + 四层升级（纯前端，后端零改动）。**① 自托管（修「设计字体从未生效」）**：全站字体此前唯一来源是 index.html 一条 Google Fonts CDN stylesheet（国内不可达），Noto Serif SC / JetBrains Mono / Cormorant Garamond / Ma Shan Zheng / ZCOOL XiaoWei 实际全在渲染系统兜底字体（Windows=SimSun/雅黑）——改为 npm 包自托管（`@fontsource/*` + `lxgw-wenkai-screen-webfont` + `misans`，均 unicode-range 分片、浏览器按需拉），新建唯一 `@font-face` 入口 `styles/fonts.css`（App.tsx 首位导入），删 index.html CDN 三行（顺带消灭一条必然超时的阻塞请求）；补上 CDN 时代缺失的 Noto Serif SC 400 字重。dist 字体产物 53MB（woff2 1075 + woff 兜底 607，rsync 增量后续不重传）。**② 令牌化收敛**：tokens.css 新增 `--jz-font-serif/sans/mono/display/kai/serif-en/decor/ui` 八令牌，全站 ~120 处硬编码 font-family（同一宋体栈曾有 6 变体）收敛为 var() 引用；JS 侧共享模块 `utils/fontStacks.ts`（常量 + `resolveFontVar`，canvas/mermaid 可导出 SVG 用实值、DOM 内联一律 var()），`fontStacks.test.ts` 读 tokens.css 锁定两边逐字一致。顺带修 5 个存量 bug：阅读正文 CSS 兜底还是旧 Verdana 栈、`.jz-meta-date` 死声明、`editor-ui.css` 族名缺引号、6 处 PingFang 领衔「伪衬线栈」、**行内码与块级码等宽栈不一致**（统一 JetBrains Mono）。**③ 新字体落位**：题记族/品牌/登录标题切 `--jz-font-display`=**霞鹜文楷 Screen**（GB 集，注册名 `'LXGW WenKai Screen'`，仅 400 字重故 display 消费点 font-weight 归一 400，两枚朱砂方印按「印章宋体厚重」留 serif）；后台 UI 切 **MiSans**（AdminLayout 挂 `<html data-scope='admin'>`，portal 弹层可解析；AntD `token.fontFamily='var(--jz-font-ui)'` var() 透传实测成立；MiSans 非标字重刻度 330/380/520/630，CSS 匹配后 400/500 坍缩同档为该刻度固有特性）。**④ 预设与排版令牌**：阅读器 `ARTICLE_FONT_PRESETS` 新增「文楷 · 屏显」（默认仍宋体、不 bump key），编辑器 `FONT_FAMILY_PRESETS` 栈与阅读端同源化（fontStacks 实值常量，存量文档内嵌旧栈不迁移）；tokens.css 新增 `--jz-fs-*`(7 档)/`--jz-lh-*`(4 档) 阶梯，首批收敛阅读基准 16.5px 三处/代码块 13px/meta 11/12px，`.jz-meta-date`/`.jz-format-pill`/统计数字补 `tabular-nums`。**已知限制**：导出端（服务器 headless Chromium）未装同款字体，PDF 字形与网页不同，待镜像重建批次处理。前端 537 测试（含 9 新增）+ tsc + build + Playwright 冒烟 16 项（文楷/Noto/JBM/MiSans 真加载、AntD var() 透传、scope 挂卸、零 Google Fonts 请求）全绿。**首页收尾补丁（同日）**：Playwright 全页字体普查后补齐首页遗留系统字体区——书卡简介/卷数 meta（+tabular-nums）/分类简介/页脚入衬线令牌；**「藏经阁」匾额是 `<h2>` 被 `.blog-content h1,h2,h3`（0,1,1）压回宋体**，补 `.blog-content .jz-hero-cangjingge` 变体抬到 (0,2,0) 修复；**裸 `<button>` 不继承字体走 UA 样式表 Arial**（主题切换钮长期 Arial）——tokens.css 补全局 `button/input/select/textarea { font-family: inherit }` reset，另 `.jz-blog-glass .jz-theme-switch` 随导航入衬线（后台经继承落 MiSans）。书卡 AntD 标签 chips 与「新建/管理」工具钮按用户拍板也入衬线（AntD css-in-js 自带 font-family，须 `.jz-book-tags .ant-tag` / `.jz-blog-home-toolbar .ant-btn` 后代选择器压过）。**后台侧栏菜单对齐修复（同日）**：图标固有宽 21–35px 不等致文字起点漂移 14px——真凶是 `.jz-admin-menu .ant-menu-item-icon { width:auto!important; min-width:0!important }`（当年为压 AntD 默认宽而写，特异性 0,3,0 反把 `.jz-menu-icon-slot` 自己的 40px 定宽压死）；改为 40px 定宽槽 + 槽内居中 + 槽内 anticon 去 12px 边距，实测 11 行图标中心均在 x=50、文字起点均在 x=88。**已推 main（880509d）并于 2026-08-10 部署上线**（标准路径：dist+backend 双 rsync 重建 caddy/backend，绿三件+文楷分片 200 实测全过） |

> **v1.0 候选**：增量自动保存 / Tiptap lazy rendering / 超大 KB 树分页 / Yjs 协作。
